# Optional dependencies for Open-A.G.I integration
# These are needed for the full infrastructure and deployment features

# Docker integration
docker>=6.0.0
docker-compose>=1.29.0

# Security and SBOM
syft>=0.80.0
cosign>=1.0.0

# Monitoring and metrics
prometheus-client>=0.15.0
psutil>=5.9.0

# Web framework for API server (if not already in requirements.txt)
fastapi>=0.95.0
uvicorn[standard]>=0.21.0
flask>=2.2.0
flask-socketio>=5.3.0

# Cryptography (if not already in requirements.txt)
cryptography>=39.0.0
pycryptodome>=3.17.0

# Testing frameworks
pytest>=7.2.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# CI/CD and build tools
build>=0.10.0
twine>=4.0.0

# Visualization and dashboard
plotly>=5.13.0
dash>=2.9.0

# Database (if needed for resource management)
sqlalchemy>=2.0.0
asyncpg>=0.27.0

# Redis client (for caching and message queuing)
redis>=4.5.0

# YAML processing
pyyaml>=6.0

# HTTP clients
requests>=2.28.0
httpx>=0.23.0

# Data processing
pandas>=1.5.0
numpy>=1.24.0

# Logging enhancements
loguru>=0.6.0

# TOR integration
stem>=1.8.0

# Machine learning (if needed for performance optimization)
scikit-learn>=1.2.0