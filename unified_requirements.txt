# Requirements for Unified Metatron-A.G.I System
# This file includes dependencies for the unified API, components, and coordination layers

# Core dependencies (already in main requirements.txt)
numpy>=1.24.0
aiohttp>=3.9.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
cryptography>=41.0.0
pydantic>=2.5.0

# Additional dependencies for unified components
cryptography>=41.0.0  # For encryption in cross-system communication
fernet>=1.0.0  # For symmetric encryption
python-socketio>=5.8.0  # For enhanced WebSocket communication
asyncio-mqtt>=0.16.0  # For MQTT-based messaging (optional)

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.11.0
flake8>=6.1.0

# Monitoring and logging
loguru>=0.7.2
prometheus-client>=0.19.0